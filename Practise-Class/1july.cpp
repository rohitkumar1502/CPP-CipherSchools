#include<iostream>
using namespace std;

int main(){
    string input_text;
    input_text = "Hi guys how is the coding coming along I hope You are having a Good Day"<<re<<endl;
    string output_text;
    // processing to output text
    re.erase(emove_if(s.begin(), s.end(),isspace),
        s.end());
    cout<<output_text;
    //we should have the spaces removed and all the text must be in lower cases


}