#include<iostream>
using namespace std;
int main(){
    // int x;
     // variable name x of type int ---> It can store interger which means x can store 1 or 2 any single storeges 
    
    // Array can be used to store multiple values in a single variable name
    

    // int a[5]; 
    // This means it is an arrays of name a which can store multiple integer , It can store 6 integer
             //Note:  the indexing of arrays start from 0 and not 1

             // element starting from 0-5 

             // The first element will have an index --> a[0]
             //The secon element will have an index 1 --> a[1]
             // and goes till a[5]

    

    int a,b,c,d,e;
    cout<<"Give 5 Integers as input"<<endl;

    cin>>a>>b>>c>>d>>e;

    cout<<"The integers in reverse order are: "<<e<<d<<c<<b<<a<<endl;
    return 0;

    
}
